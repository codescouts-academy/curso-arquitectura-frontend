{
    "name": "@codescouts/monorepo",
    "private": true,
    "version": "0.0.1",
    "engines": {
        "npm": "7.24.2",
        "node": "16.13.0"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "kill-port": "kill-port --port 3000",
        "start": "npm run kill-port && lerna run --parallel start"
    },
    "devDependencies": {
        "kill-port": "^2.0.1",
        "lerna": "^6.0.1"
    }
}